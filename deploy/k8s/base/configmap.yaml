apiVersion: v1
kind: ConfigMap
metadata:
  name: triton-server-config
data:
  # Triton 서버 추가 인자 — 환경별 overlay에서 덮어쓰기
  TRITON_ARGS: >-
    --model-control-mode=explicit
    --strict-model-config=true
    --log-verbose=1
    --allow-metrics=true
    --allow-gpu-metrics=true
