# =============================================================================
# HTTP TLS — 인증서 기반 보안 통신 (HTTP/REST)
# =============================================================================

--http-ssl-certfile=/certs/server.crt
--http-ssl-keyfile=/certs/server.key
