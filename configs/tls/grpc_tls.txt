# =============================================================================
# gRPC TLS — 인증서 기반 보안 통신 (gRPC)
# =============================================================================
# 사전조건: 인증서 파일이 지정 경로에 존재해야 함
# 인증서 생성 절차: configs/tls/README.md 참조
# =============================================================================

--grpc-use-ssl=true
--grpc-server-cert=/certs/server.crt
--grpc-server-key=/certs/server.key
--grpc-root-cert=/certs/ca.crt
