# Model conversion dependencies for deploy/docker/Dockerfile.converter.
#
# These versions are pinned for reproducible CI converter images. Revisit as a
# set when changing the Triton base image version.
torch==2.5.1
onnx==1.17.0
onnxruntime==1.20.1
xgboost==2.1.3
lightgbm==4.5.0
scikit-learn==1.6.0
PyYAML==6.0.2
